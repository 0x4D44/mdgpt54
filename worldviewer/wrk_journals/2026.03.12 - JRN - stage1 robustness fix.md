# 2026-03-12 Stage 1 robustness fix

## 13:13
- Task: final Stage 1 robustness fix for null `canvas.getContext("2d")` handling in `src/traffic/trafficLayers.ts`.
- Constraint check: no commit, no supervision journal edits, stay inside the current working tree.
- Plan: patch the icon creation fallback only, then add one small regression test if the module is easy to isolate.
- Emotional temperature: calm, slightly cautious because `trafficLayers.ts` already has in-flight edits from earlier work.

## 13:13
- Implemented the smallest safe fallback: return a transparent image payload when the 2D context is unavailable instead of throwing through `addTrafficLayers`.
- Added one focused regression test that stubs `document.createElement("canvas")` to return a null `getContext("2d")` result and verifies icon registration still succeeds.
- Validation: targeted `vitest` run passed, `npm test` passed, and `npm run build` passed. Build still emits the existing Vite chunk-size warning.
- Emotional temperature: relieved; the path was cheap to test without dragging in broader UI setup.
