2026-03-13 18:20 Europe/London

Task
- Arthur reports that aircraft shrink while zooming in and then disappear.

Supervisor notes
- Prior read-only investigation points at the 2D to 3D aircraft handoff happening too early, not at projection math or map tile failures.
- I am keeping context narrow and delegating implementation plus an adversarial review before accepting that diagnosis.

Emotional temperature
- Cautious. The symptom is visually obvious, but these rendering issues are easy to misdiagnose if we stack fixes.

Next actions
- Send one implementation agent to make the smallest credible fix with focused tests.
- Send one devil's-advocate agent to challenge the root-cause assumption and call out anything the implementer misses.

## 2026-03-13 17:23 Europe/London

- Root cause confirmed: this is still the early 2D-to-3D handoff, not a tile-loading problem. The 3D activation zoom was `10.5`, which is far too early for meter-true procedural aircraft to read at screen size.
- Tight fix only: raised the aircraft 3D zoom hysteresis in `src/traffic/aircraft3d.ts` from `10.5/10` to `13.5/13`. Pitch and visible-count gates stayed unchanged.
- Added focused regression coverage so the handoff now proves:
  - pure mode logic stays 2D below `13.5`
  - controller logic does not lazy-load `three` or hide 2D aircraft below `13.5`
  - hysteresis still holds once 3D is active
- Validation:
  - `mdtimeout 120s cmd /c npx vitest run src/traffic/aircraft3d.test.ts src/traffic/aircraft3dLayer.test.ts`
  - `mdtimeout 180s cmd /c npm run build`
- Result: both passed. Build still emits the pre-existing Vite chunk-size warning.
- Feeling: satisfied with the scope. This is the smallest credible fix for the observed symptom, and it avoids inventing a scaling hack before proving one is needed.

## 2026-03-13 17:38 Europe/London

- Feedback item 1: agree. The bad path was the handoff itself: `Aircraft3dController` hid all renderable 2D aircraft as soon as global 3D mode enabled, even when the replacement mesh was still too small to read. I left the 3D transform math untouched.
- Feedback item 2: agree. The `13.5/13` zoom hysteresis is not enough on its own because Mercator meters-per-pixel varies by latitude and aircraft classes have materially different physical sizes. I kept the existing global gates only as coarse activation guards for pitch/count/churn, not as the sole hide decision.
- Feedback item 3: partially agree with the suggested fix and implemented the minimal version. I added a per-track handoff eligibility check in `src/traffic/aircraft3d.ts` that estimates on-screen size from `zoom + latitude + class` using representative class dimensions and Web Mercator meters-per-pixel. `src/traffic/aircraft3dLayer.ts` now renders and hides only that eligible subset, so unreadable 3D replacements no longer suppress their 2D markers.
- Scope control: I did not build a generic LOD system, did not change transform math, and did not broaden the count heuristic. The change stays narrowly focused on preventing invisible or unreadable swaps.
- Tests added: unit coverage now proves latitude and class both affect handoff eligibility at the same zoom (`narrow-body` at high latitude hands off sooner than at the equator; `bizjet` stays 2D where `wide-body` can hand off). Controller coverage now proves a small bizjet stays 2D even when global 3D mode otherwise qualifies.
- Validation:
  - `mdtimeout 120s cmd /c npx vitest run src/traffic/aircraft3d.test.ts src/traffic/aircraft3dLayer.test.ts`
  - Outcome: passed, `2` test files and `25` tests green.
  - `mdtimeout 180s cmd /c npm run check`
  - Outcome: passed, TypeScript checks clean for app and scripts.
- Feeling: more confident than the earlier threshold bump. This version is still small, but it finally keys the hide decision to screen readability instead of a global guess.

## 2026-03-13 17:48 Europe/London

- Review follow-up pass on the aircraft zoom handoff diff only. I kept the scope to tests plus this journal entry because the review findings were about regression coverage, not runtime behavior.
- Required change 1: agree and fixed. In `src/traffic/aircraft3d.test.ts` I moved the count-above-on-threshold case from `zoom: 11` to `zoom: 13.5`, and the pitch-off assertion from `zoom: 11` to `zoom: 13.2`, so those assertions now fail only when count or pitch gating breaks.
- Required change 2: agree and fixed. In `src/traffic/aircraft3dLayer.test.ts` I added a mixed controller regression with one eligible narrow-body aircraft and one ineligible bizjet, proving only the eligible ID is hidden after the 3D layer loads.
- Optional suggestion about consolidating aircraft size assumptions: skipped. It is a reasonable cleanup, but it would touch stable geometry code for no correctness gain in this pass.
- Reviewer question on count hysteresis using `renderableTracks.length` instead of `handoffTracks.length`: my answer is yes, that still looks intentional. The count gate is acting as a coarse global churn/performance guard for overall 3D mode, while `handoffTracks` decides per-aircraft readability and hiding.
- Reviewer question on the `6px` cutoff: I do not see evidence of calibration in the code. It reads as a heuristic readability threshold introduced with the handoff filter.
- Validation:
  - `mdtimeout 120s cmd /c npx vitest run src/traffic/aircraft3d.test.ts src/traffic/aircraft3dLayer.test.ts`
  - Outcome: passed, `2` test files and `26` tests green.
- Feeling: this was the right kind of follow-up. The review caught real blind spots in the tests without pulling the implementation back into churn.

## 2026-03-13 18:10 Europe/London

- Final completion audit after the shared icon-sizing calibration came back `COMPLETE`.
- Devil's-advocate challenge raised one last concern: `AIRCRAFT_3D_OFF_ZOOM` is still `13`, so previously-enabled 3D can remain active in the `13.0-13.5` band.
- I treated that as a tie-break question instead of thrashing the code. Final review result: not critical. The per-track size floor is now the real hide gate, so aircraft only stay in 3D there when the replacement is already at least as large as the capped 2D symbol.
- Validation status at close:
  - targeted agent-run Vitest slices passed
  - `npm run check` passed
- Emotional temperature: satisfied, but only after the extra audit loop. The first “done” was too optimistic.

## 2026-03-13 18:02 Europe/London

- Final completion audit feedback item 1: agree and fixed. The `6px` handoff floor was still an uncalibrated heuristic, and it could replace the capped 2D aircraft symbol with a visibly smaller 3D mesh. I kept the existing per-track readability model but retuned the floor to the actual 2D aircraft symbol size already used by `src/traffic/trafficLayers.ts`.
- Implementation detail: I pulled the aircraft icon sizing constants into `src/traffic/aircraftIconSizing.ts`, reused them from `src/traffic/trafficLayers.ts`, and set the 3D handoff floor in `src/traffic/aircraft3d.ts` to the capped 2D symbol size: `48px / pixelRatio 2 * icon-size 0.58 = 13.92px`.
- Final completion audit feedback item 2: agree and fixed. The focused tests now prove the new expectation instead of the old `6px` heuristic:
  - `src/traffic/aircraft3d.test.ts` now shows that at `zoom 13.5` only a high-latitude wide-body is large enough to hand off, while a high-latitude narrow-body still stays 2D; that same narrow-body only hands off at `zoom 14`.
  - `src/traffic/aircraft3dLayer.test.ts` now keeps a narrow-body visible at `zoom 13.5` and hides only an eligible wide-body in the mixed-controller case.
- Scope held: no crossfade, no dynamic LOD framework, no transform changes. This stayed a constant retune plus test evidence.
- Validation:
  - `mdtimeout 120s cmd /c npx vitest run src/traffic/aircraft3d.test.ts src/traffic/aircraft3dLayer.test.ts src/traffic/trafficLayers.test.ts`
  - Outcome: passed, `3` test files and `27` tests green.
  - `mdtimeout 180s cmd /c npm run check`
  - Outcome: passed.
- Emotional temperature: calmer now. This is the smallest fix that actually matches the current 2D visual baseline instead of guessing.
