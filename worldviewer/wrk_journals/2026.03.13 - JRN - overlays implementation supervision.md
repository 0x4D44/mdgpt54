# Overlays Implementation Supervision Journal

## 2026-03-13 12:32

- Arthur asked for supervised implementation with an agent team rather than a single monolithic coding pass. That is the right shape for this feature because the work is split cleanly into slices and the design already has a review trail.
- I loaded the `code-reviewer` and `code-review-apply-feedback` skills up front so review and feedback application stay structured instead of becoming vague commentary after the fact.
- I also checked for actual local agent tooling. Both `codex` and `claude` CLIs are available, which means I can delegate real implementation and review passes instead of pretending the team exists.
- Stage plan:
  1. presets + overlay scaffolding + solar terminator
  2. weather radar
  3. cross-feature review and devil's-advocate pass
  4. final completeness audit against the HLD
- Commit discipline matters here. Each stage should end with tests/checks and its own atomic commit so the review trail stays readable.
- Feeling: focused and guarded. The main failure mode now is losing discipline and letting one stage bleed into the next.

## 2026-03-13 12:49

- Stage 1 scope is narrower than the full HLD: presets, the scene note/credit hooks, the anchor helper, and the solar terminator only. Weather stays out. That cut matters because it keeps `main.ts` from turning into a half-finished overlay framework.
- The on-disk `AGENTS.md` file Arthur referenced is missing from the workspace root, so I am treating the prompt instructions as authoritative and not wasting time chasing a phantom file.
- Existing tests are plain `vitest` with light fakes rather than browser integration. That pushes the right implementation shape: pure helpers for anchor lookup and solar math, then one small stateful overlay wrapper with mocked map calls.
- There are unrelated worktree changes already present in `package-lock.json` and docs/journal files. I am leaving them alone.
- Feeling: steady, mildly suspicious of dateline edge cases, and happier once the math is under test before I wire it into the map.

## 2026-03-13 13:01

- Stage 1 implementation is in: presets replaced, `Night` added to Scene, scene overlay note/credit placeholders wired, and the solar terminator lives in its own small module under `src/overlays`.
- I kept the overlay ownership local on purpose. There is no shared overlay manager, only a `createSolarTerminatorOverlay()` wrapper with idempotent `enable` / `disable`, a load-event wait path, and a one-minute refresh timer.
- `npm run check` passes cleanly, which is the meaningful static signal here.
- Both `npm test` and `npm run build` are blocked by the sandbox, not by TypeScript. Vite/Vitest hit `spawn EPERM` inside Windows-specific path/config handling before they can finish. I verified that trying the experimental native config loader changes where it fails, but not the outcome.
- Feeling: slightly irritated by the sandbox false negatives, but the code shape itself is disciplined and small. The real remaining risk is visual seam behavior around the dateline, which needs the manual globe check the HLD already called out.

## 2026-03-13 13:42

- Stage 1 feedback application pass focused on solar terminator correctness only; weather stayed untouched.
- I fixed the negative-declination geometry bug in `solarTerminator.ts` by forcing terminator latitude onto the principal `[-90, 90]` branch after `atan2`, which preserves sunlit/night semantics for both solstice signs.
- I also applied a small lifecycle safety improvement: pending `load` handlers now track their owning map (`loadHandlerMap`) so cross-map re-enable cannot leak an old listener.
- Tests were strengthened in `solarTerminator.test.ts` with a December solstice declination check and semantic night/day assertions across both June and December dates so the previous broken winter branch is covered.
- Required cleanup item hit a filesystem permission wall: this sandbox user can write files but cannot delete/rename these temp artifacts (Windows returns `Access is denied` / git reports `Invalid argument` on removal). I left no new temp files but could not physically remove the pre-existing ones.
- Validation: `npm run check` passed; `npm test` and `npm run build` both failed at Vite config startup with `spawn EPERM` (esbuild child-process spawn restriction), consistent with sandbox constraints rather than TypeScript errors in touched Stage 1 files.
- Feeling: satisfied with the math fix, frustrated by deletion and spawn restrictions because they mask clean completion signals and make hygiene tasks look unfinished.

## 2026-03-13 14:17

- Fresh Stage 1 rereview was worth it. One reviewer still found a concrete antimeridian seam concern in the solar geometry, and the devil's-advocate pass correctly called out that `findFirstLabelLayerId()` violates the HLD by preferring a known-id table over actual style order.
- My tie-breaks: anchor ordering is a real blocker and must be fixed. The solar review needs another implementation pass too, but I do not want a speculative rewrite; the next agent should either make the seam/equinox behavior explicitly correct and testable or push back with evidence if the reported seam is only a probe artifact.
- I am intentionally not broadening scope. Weather remains Stage 2. I also do not want a generic overlay manager or any extra compatibility machinery creeping in while Stage 1 is still being stabilized.
- Feeling: more confident in the supervision loop now. The reviews are surfacing real issues instead of churn, which is exactly the point of splitting implementation from review.

## 2026-03-13 14:24

- Stage 1 feedback application is now in. I changed the anchor helpers so they scan the style in order and treat known ids as recognition hints instead of a priority override. That closes the concrete `findFirstLabelLayerId()` blocker and keeps the same bug from leaking into the road/satellite helpers later.
- I did not rewrite the solar geometry. I probed it directly and the supposed seam failure only reproduced at exact `lng = 180` with the old test-side planar point-in-ring check. The production geometry still classified just-off-seam samples correctly, and a seam-aware unwrapped probe showed zero mismatches against the solar illumination sign across solstices and across the late-March declination sign flip.
- Based on that, the right fix was stronger tests, not more production math. `solarTerminator.test.ts` now unwraps seam-crossing rings before ray-casting and adds regression coverage for exact `±180`, `±179.999`, and dates on both sides of the March sign flip.
- I also tightened the HLD text so it no longer implies the wrong anchor priority and so the solar validation section explicitly calls for seam-aware antimeridian coverage.
- Feeling: relieved. This was exactly the kind of review item that can trigger a noisy rewrite if nobody stops to prove what is actually broken.

## 2026-03-13 08:25

- Minimal Stage 1 hardening only. The concrete bug here was that missing solar layer anchors fell through to `addLayer(..., undefined)`, which makes the night fill topmost.
- I fixed it in `solarTerminator.ts` with a narrow fallback chain: label anchor first, then road anchor, then the first style layer. That is safer than top-of-stack without dragging in another shared helper.
- Regression coverage now includes a style with no label or road anchor so the overlay must insert before the first existing layer instead of floating above everything.
- I did not touch the HLD for this pass. The fallback is a defensive implementation detail, not a change in overlay design.
- Feeling: calm and a bit annoyed that the bad path is invisible on happy-path styles, but this is exactly the kind of small hardening pass that pays for itself.

## 2026-03-13 14:39

- Stage 1 blocker fix: the prior `layers[0]` fallback was wrong because it can bury the solar night mask under the basemap. I replaced it with a small helper that skips base-obscuring `background`/`fill`/`hillshade`/`raster` layers and only leaves `beforeId` undefined when the style has nothing better than top-of-stack.
- Tests now prove the no-label/no-road path anchors before the first non-base content layer instead of under the background stack.
- Feeling: better. This is the narrowest change that fixes the visibility bug without inventing a broader overlay-placement policy.

## 2026-03-13 08:45

- Final Stage 1 blocker was real: the fallback helper still returned the first non-base layer anywhere in the style, which breaks on interleaved stacks when a later obscuring base layer resumes above early content.
- I kept the fix local in `overlayAnchors.ts`: the fallback now inserts before the first layer after the last obscuring `background`/`fill`/`fill-extrusion`/`hillshade`/`raster` layer, and still returns `undefined` when every layer is obscuring base content.
- Added one interleaved regression at the helper level and mirrored it through the solar overlay anchor test so the failure mode is pinned from both sides.
- Feeling: satisfied. This closes the Stage 1 placement ambiguity without widening policy or touching unrelated overlay code.

## 2026-03-13 16:07

- Stage 1 is committed as `3b18672`. The loop was messy, but the final state is sharper because the reviewers forced every "fallback" to earn its behavior instead of hand-waving it away.
- The useful lesson from Stage 1 is that placement rules are feature logic, not implementation detail. Weather needs the same discipline from the start or it will drift into another round of late anchor fixes.
- Stage 2 starts now and stays narrow: RainViewer metadata fetch, raster source/layer update flow, note/credit wiring, and async lifecycle tests. No animation, no framework, no extra branches.
- Feeling: slightly tired from the repeated solar passes, but clearer-headed about where the real risks are.

## 2026-03-13 09:10

- Stage 2 weather radar implementation is in. I added a dedicated `weatherRadar.ts` module with pure helpers for RainViewer metadata parsing, tile-url construction, and fixed UTC status formatting, plus a small stateful wrapper that owns the fetch timer, `AbortController`, load-handler cleanup, and source/layer lifecycle.
- `main.ts` stayed small on purpose: one `Weather` scene chip (default off), one local weather presentation snapshot, and one render function that composes the existing night note with the async weather note/credit. No generic overlay bus, no presets churn, no solar rewrites.
- The overlay uses the reviewed RainViewer tile suffix from the earlier design trail (`2/1_0`) and updates existing raster tiles through the public `setTiles()` API rather than touching private MapLibre state. Failed or malformed metadata now removes the layer/source and reports `Radar unavailable`, which keeps stale weather off the map.
- Tests now cover the meaningful Stage 2 branches: metadata parsing, URL formatting, fixed UTC note formatting, `setTiles()` refresh behavior, malformed metadata cleanup, request-failure fallback, disable-while-fetching race protection, and idempotent repeated enable on the same map.
- Validation result is mixed but clear. `npm run check` passed after one small test-type fix. `npm run test -- src/overlays/weatherRadar.test.ts src/overlays/overlayAnchors.test.ts` failed before running any tests because Vite/esbuild still hits sandbox `spawn EPERM` while loading `vite.config.ts`, the same class of blocker Stage 1 saw.
- Feeling: reasonably satisfied. This stayed disciplined and local. The remaining frustration is not code shape; it is the sandbox hiding the actual test runtime behind a startup restriction I cannot fix from inside the repo.

## 2026-03-13 16:34

- Stage 2 devil's-advocate review is complete against `src/main.ts`, `src/overlays/weatherRadar.ts`, `src/overlays/weatherRadar.test.ts`, `src/overlays/overlayAnchors.ts`, `src/overlays/solarTerminator.ts`, and the reviewed HLD at `wrk_docs/2026.03.13 - HLD - overlays and presets.md`.
- One required bug survived the current tests: the weather refresh loop only guards async completion by enable/disable revision, not by per-refresh ordering. If an older in-flight metadata request resolves after a newer one in the same enabled session, it can overwrite fresher tiles or flip the UI back to `Radar unavailable`.
- The smaller lifecycle concern is style-reset resilience. `weatherRadar.enable(map)` short-circuits on the same map, so it cannot re-assert its source/layer if they disappear while the toggle remains on. That is not triggered by the current app flow, but it is the main hidden assumption left in the weather path.
- Test coverage is decent for the narrow happy paths and disable race, but it still lacks a same-session out-of-order refresh test and any weather equivalent of the solar style-load handler tests.
- Feeling: satisfied with the review quality, mildly annoyed that the strongest bug is exactly the kind of race that looks fine until you reason about time instead of lines of code.

## 2026-03-13 09:36

- Stage 2 feedback application stayed local to `weatherRadar.ts` and its test file. I added one refresh-sequence counter so stale same-session completions cannot overwrite newer weather state even if an older fetch ignores abort.
- I also loosened the same-map `enable()` early return: if the radar source/layer vanished, the overlay now reasserts itself from the cached tile URL and immediately refreshes instead of silently doing nothing forever.
- Tests now cover both regressions directly: out-of-order same-session completions and same-map re-enable after losing style state.
- Validation is next. I expect `npm run check` to be meaningful; Vitest may still hit the known Windows sandbox `spawn EPERM` before execution.
- Feeling: better. This is the right size of fix and it closes the race without turning the overlay into a mini state machine.

## 2026-03-13 17:07

- Stage 2 is committed as `91335d5` after the follow-up rereview came back with no required changes. The weather slice now has its own clean checkpoint instead of living in limbo with the solar work.
- I am moving into the cross-feature pass now. The focus shifts from local weather correctness to interaction quality across presets, Night, Weather, scene notes/credits, and the leftover Stage 1 lifecycle/test nudges that were intentionally deferred.
- The main thing I want from Stage 3 is confidence that the overlay family is cohesive rather than merely individually decent. If there is still a hidden assumption, it is probably in the seams between modules rather than inside one module.
- Feeling: steadier. The stage boundary matters; the codebase is easier to reason about once each slice has been forced through review and frozen in a commit.

## 2026-03-13 17:18

- Stage 3 feedback application stayed narrow. I agreed with the weather lifecycle gap and added only the missing pre-style-load regressions in `weatherRadar.test.ts`, mirroring the existing solar contract instead of changing runtime behavior.
- The two added cases cover: waiting for first style load before any weather fetch/source/layer work, and canceling the pending load path when Weather is disabled before style load so a later `load` event cannot fetch, add, or publish.
- No production code changed because the current `weatherRadar.ts` path already appears to satisfy that contract, and the supervisor tie-breaks correctly ruled out speculative load/style recovery work.
- Feeling: calm. This was the right kind of feedback item because it tightened the contract without dragging the feature into another rewrite cycle.
