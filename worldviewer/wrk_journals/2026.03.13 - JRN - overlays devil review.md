## 2026-03-13 14:35

- Task: adversarial review of the overlays implementation across `src/main.ts`, overlay modules/tests, and the overlays HLD.
- What I checked: lifecycle edges (`enable`/`disable` before load), recovery from source/layer loss, scene-note state derivation, anchor heuristics/fallbacks, and whether the implementation stayed smaller than the HLD justified.
- What I learned: the weather overlay contains explicit same-map reassertion logic, but `main.ts` never replays `enable()` when style state disappears, so the reviewed recovery path is not actually wired into the app. The night overlay is simpler, but its UI note is still driven by desired state instead of rendered state.
- Validation: `npm run check` passed. `npm test` did not run in this sandbox because Vitest/Vite failed during startup with `Error: spawn EPERM` while loading `vite.config.ts`.
- Risks I plan to surface: stale scene-note/credit text when overlay state drifts from map state, unproved weather disable-before-load behavior, and road-anchor heuristics that are more style-name-dependent than the HLD claims.
- Emotional temperature: skeptical. The implementation is close, but a couple of tests are proving helper behavior in isolation or proving call patterns the app does not actually emit, which is the exact kind of thing that slips through a final audit.

## 2026-03-13 15:11

- Task: second-pass hostile audit focused on disproving completion claims against `wrk_docs/2026.03.13 - HLD - overlays and presets.md`.
- What I checked: exact HLD validation bullets versus implemented code/tests, projection hysteresis against the night-opacity curve, and whether any journal/doc artifact actually records the required manual checks.
- What I learned: the biggest real code mismatch is the projection seam. `src/main.ts` keeps Mercator active until zoom drops back to `5`, but `src/overlays/solarTerminator.ts` only fades the night mask to zero at zoom `6`, so the night overlay can still be faintly visible on Mercator in the `5..6` band after zooming back out. The HLD explicitly framed this work as avoiding Mercator/night misuse, so that gap is not just cosmetic.
- Validation: `mdtimeout 45s npm.cmd run check` passed. Targeted Vitest still fails before execution with the known Vite/esbuild `spawn EPERM` startup error, so automated runtime proof is unavailable from this sandbox.
- Risks I plan to surface: the HLD's required manual checks are still not evidenced anywhere I searched, and another journal explicitly says the dateline and zoom-boundary checks were not exercised from the sandboxed pass.
- Emotional temperature: more convinced than before that "implementation present" is not the same as "ready to call complete." The code is mostly there, but the last mile is exactly where teams lie to themselves.

## 2026-03-13 18:42

- Task: final devil's-advocate completion audit after the Mercator/Night fix, using the reviewed HLD as source of truth and limiting scope to the listed overlay/preset files.
- What I checked: exact preset table values, Scene chip defaults, scene note/credit placement, projection-gated Night wiring, weather layer/source contract, anchor-helper order rules, overlay lifecycle/load/disable paths, and whether any remaining concern was a real HLD mismatch versus a sandbox validation gap.
- What I learned: the earlier Night/Mercator seam is actually closed now. `src/main.ts` drives Night from `shouldShowNightOverlay(nightEnabled, projectionMode)`, projection hysteresis stays at `5`/`6` in `src/projectionBehavior.ts`, and `updateProjectionMode()` re-syncs overlays on real projection flips. Weather still uses public `setTiles()` rather than private caches, carries RainViewer attribution on the raster source, and honors the `maxzoom: 8` render cutoff the HLD asked for.
- Validation: `mdtimeout 120000 npm.cmd run check` passed. Targeted Vitest still failed before test execution with the known Vite/esbuild `spawn EPERM` startup error while loading `vite.config.ts`, so I treated runtime execution as a sandbox limitation rather than feature evidence. I also tried direct TypeScript runtime probes, but the same sandbox blocks esbuild-backed tooling there too.
- Remaining caution: the manual globe/dateline inspection remains unproved from this environment, and the weather note/credit text is keyed to overlay activity rather than current zoom visibility. I do not think either rises to a real post-fix HLD blocker.
- Emotional temperature: still hostile to completion claims by default, but at this point I cannot justify a `NOT COMPLETE` verdict from the code that is actually present.
