## 2026-03-12 Initial pass

- Task: implement Step 3 bounded 3D aircraft rendering from the aircraft HLD, scoped to hysteresis, altitude/class selection, and a bounded MapLibre custom layer.
- Context checked: HLD, `src/traffic/trafficTypes.ts`, `src/traffic/openskyDirect.ts`, `src/traffic/trafficHelpers.ts`, `src/traffic/trafficLayers.ts`, `src/main.ts`, existing tests, and package scripts.
- Immediate design read: Step 2 already enriches `renderModelKey`, so Step 3 should consume that and only fall back to coarse class logic where necessary. No second aircraft pipeline is justified.
- Constraints I am keeping tight: no ship changes, no Step 2 rewrite, no asset pipeline, no empty transition while three.js loads, and visible-count must come from raw tracks filtered by viewport.
- Feeling: the surface looks manageable if I keep the pure logic separate from MapLibre/three wiring. The obvious failure mode is letting the rendering layer sprawl into `main.ts` or re-deriving aircraft identity in multiple places.

## 2026-03-12 TDD red

- Added failing tests for a new `aircraft3d` decision module covering activation thresholds, hysteresis, altitude choice, and class selection.
- Added failing expectations for OpenSky `onGround` parsing so grounded aircraft can stay on the 2D symbol path.
- Scoped red result is clean: one missing module and one missing field, which is exactly the narrow seam I wanted before moving into implementation.
- Feeling: good. The failure shape is honest and not noisy. Next risk is runtime integration, not the decision logic.

## 2026-03-12 Review feedback application

- Applied the agreed Stage 3 review fixes without widening scope beyond the aircraft 3D path.
- Tightened type-code classification by replacing the broad `C1`..`C7` bizjet prefixes with Citation-family prefixes, removing the bogus `C25B` prop match, adding `C130` to the prop path, and adding a small helicopter prefix list (`R44`, `EC35`, `B429`, etc.) before descriptor/category fallback.
- Reworked the custom-layer placement path to stop casting through `map.transform`. The layer now uses the public custom-layer render inputs plus `MercatorCoordinate.fromLngLat(...)` for model transforms, which is the right fit for the current high-zoom mercator-only 3D path.
- Stopped the lazy `three` load from retrying forever after failure. The controller now records a one-time failure, logs one warning, and leaves 2D aircraft visible instead of churning on every sync.
- Cleaned up `syncAircraftObjects` so it no longer deletes from the object map while iterating that same map, and dropped the misleading `antialias` flag when reusing MapLibre's GL context.
- Added focused regression coverage for the false-positive type-code cases and the one-shot `three` import failure behavior.
- One narrow regression surfaced during the focused pass: I initially narrowed Citation prefixes too far and dropped `C56X`. The focused test caught it immediately, I added that family back, and the rerun went green.
- Validation:
  - `npx vitest run src/traffic/aircraft3d.test.ts src/traffic/aircraft3dLayer.test.ts`
  - `npm test`
  - `npm run build`
- Validation result: all requested checks passed. The existing Vite chunk-size warning still appears and remains out of scope for this review-follow-up slice.
- Feeling: relieved more than excited. This was the right kind of feedback pass: real correctness fixes, one honest regression caught by the new tests, and no drift into a broader rendering rewrite.

## 2026-03-12 Second narrow feedback pass

- Applied the second Stage 3 feedback pass only in `src/traffic/aircraft3dLayer.ts` and `src/traffic/aircraft3dLayer.test.ts`.
- I explicitly disagreed with the claimed `clone(true)` GPU leak and checked the installed `three` source before touching code. In this version `Mesh.copy` shares `geometry` and `material`, so I left stale-clone disposal alone and added a short comment near clone creation to make that assumption visible.
- I agreed with the real geometry issue and fixed it by making the procedural aircraft convention internally consistent: fixed-wing meshes now use local `+Y` as nose-forward and `+Z` as up, instead of rotating only the fuselage and leaving the rest of the model on a different axis.
- I simplified the model matrix to match that convention: translation, heading rotation, then mercator scale with negative Y. The per-frame helper now writes into the target matrix and reuses scratch `Matrix4` instances instead of allocating several matrices per aircraft per frame.
- I moved object membership reconciliation out of `render()` and into `setTracks()`. That keeps the bounded behavior but stops rebuilding the object-id set every render frame.
- I also fixed the latent class-refresh bug: if a track keeps the same id but its `classKey` changes after identity enrichment, the runtime layer now replaces the mesh instance instead of silently keeping the old class.
- Added narrow regression coverage for:
  - fixed-wing nose/fuselage axis alignment
  - mesh replacement on `classKey` change
  - model-matrix heading orientation
- Validation:
  - `mdtimeout 120s cmd /c npx vitest run src/traffic/aircraft3d.test.ts src/traffic/aircraft3dLayer.test.ts` passed
  - first `mdtimeout 180s cmd /c npm test` run failed while I had `npm run build` going in parallel; the visible symptoms were one unrelated timeout in `trafficLayers.test.ts` followed by esbuild `cannot allocate memory` / `EPIPE`
  - retry `mdtimeout 240s cmd /c npm test` passed cleanly
  - `mdtimeout 180s cmd /c npm run build` passed; the existing Vite chunk-size warning remains
- Feeling: this was a better pass than the first one because it cut real per-frame churn instead of adding more machinery. Slightly annoyed at myself for parallelizing the full test/build sweep and provoking a fake memory failure, but the rerun made the signal clear.

## 2026-03-12 Final tightening pass

- Added one last success-path controller test so the 3D load path is no longer inferred only from the failure-path test.
- Added one direct lowercase type-code regression in `aircraft3d.test.ts` so the classification path now proves it is insulated from casing drift in upstream identity data.
- The first validation run showed the new success-path controller test was under-flushed, not that the controller was broken. I fixed the shared frame-drain helper to wait through a few idle microtask passes so async `loadThree().finally(scheduleSync)` work is actually observed in the test.
- This was intentionally tiny. The goal was to remove the last plausible "yes, but is that assumption tested?" argument without reopening the rendering code again.
- Feeling: done enough. These are the kinds of last checks that make the final audit less argumentative.
