# 2026-03-11 Live Traffic Supervision Journal

## 23:38

- Arthur wants the live traffic feature implemented by an agent team with supervision, staged commits, independent review, a devil's-advocate pass, and a final completeness audit.
- Confirmed the local environment has `claude`, `gemini`, and `codex` CLIs, so this can be a real delegated workflow rather than roleplay.
- Also confirmed the review companion skill is named `code-review-apply-feedback` here, not `code-review-apply-changes`. I will use the available skill and call out the mapping when needed.
- Current repo state is almost clean, but the HLD and the main build journal still have uncommitted changes from the design-refinement step. I need to commit that as the stage-0 baseline before implementation agents start writing code on top of it.
- Feeling: focused and slightly wary. Multi-agent work is useful, but it gets sloppy fast if the stages and branches are not controlled tightly.

## 23:47

- Switched into backend feedback-application mode for the live traffic relay. Scope is backend only, no frontend or map-layer work, and no commit.
- The review feedback is sound. The current relay can silently leave stale shared provider state in play when a bbox is too large, restart OpenSky too aggressively after subscriber churn, and spam AISStream resubscriptions on unrelated changes.
- The cleanest minimal fix looks like a small relay-core extraction that owns subscription resolution, layer status, shared bbox calculation, stale expiry, and the OpenSky timing floor. That keeps the websocket bootstrap thin and makes the risky logic testable.
- I am mildly irritated that the current tests stop at helper parsing, because the bugs are all in the state machine, but at least the surface area is still small enough to correct without a broad refactor.

## 23:55

- Created a dedicated backend worktree and delegated the server-side slice to Claude. Good result on the first pass: clean `server/` layout, provider adapters, relay skeleton, tests for the pure helpers, and minimal package/runtime wiring.
- Ran a separate devil's-advocate review agent over that worktree before letting anything merge. That review found real issues, not cosmetic ones: oversized union bboxes were not rejected cleanly, OpenSky churn could violate the stated polling floor, AIS resubscribe behaviour was too noisy, the browser contract was missing status/capability semantics, and the relay logic itself had no tests.
- This is exactly why the supervision layer exists. The backend slice is not ready to integrate yet, but it is close and the next move is clear: push those findings through an apply-feedback agent instead of patching them by hand.
- Feeling: satisfied with the process so far. The team actually caught meaningful problems before they leaked into the main branch.

## 00:11

- The relay now resolves effective layers per client instead of assuming every requested layer is accepted. Oversized aircraft or ship requests get empty arrays plus explicit `zoom_in` status, and missing AIS credentials produce `unavailable` for ships.
- OpenSky polling now carries the last poll timestamp through zero-subscriber churn, so a restart can wait out the remaining 10-second floor instead of polling immediately again.
- AISStream reconnect/resubscribe logic now keys off the shared ship bbox changing, which removes the obvious resubscribe spam on aircraft-only churn.
- Added focused tests for validation, shared-session filtering, oversize refusal, stale ship expiry, OpenSky timing, and the ship heading fallback to course over ground.
- Feeling: better. This is still not a perfect fairness policy for conflicting oversized subscriptions, but it is honest, bounded, and tested instead of silently wrong.

## 00:25

- Independent validation is clean: the server typecheck passes and the backend test suite is green after the feedback pass.
- The formal review found one real correctness bug left in the AIS adapter: malformed messages without MMSI could create a phantom ship id of `undefined`. I pushed that back through a separate apply-feedback agent instead of hand-waving it away.
- I also accepted the small `shipNames` eviction cleanup because it was cheap and keeps the in-memory state honest during long sessions. I rejected the temptation to churn comments or speculative refactors just because review surfaced them.
- One process catch on my side: `package.json` had not been version-bumped despite a genuine new feature slice. Corrected that now before the backend-stage commit.
- Feeling: more comfortable. This slice now looks like a disciplined backend increment rather than an enthusiastic prototype.
