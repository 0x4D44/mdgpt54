# 2026-03-12

- Task: Implement Step 2 from the aircraft identity / 3D HLD without touching Step 3.
- Context: Stage 1 is already present in the shared `LiveTrack` path. The supervision journal is dirty, so I am leaving it alone and keeping notes here.
- Plan: Add a small local generator, build the real static shards from the verified OpenSky complete snapshot, then wire a session-only lookup into the browser poll path.
- Insight: The cleanest join point is `TrafficClient`, because that already owns the visible aircraft fetch cycle and can re-publish a snapshot after identity shards arrive.
- Risk: The generator needs to be streaming and CSV-robust enough for the OpenSky snapshot. A naive split on commas is a bad idea.
- Emotional temperature: Focused, slightly wary of the CSV edge cases, otherwise straightforward.

- Progress: Added a typed generator script, a session-only browser identity cache, popup wiring, README/credit updates, and focused generator/runtime tests.
- Data source used: `https://s3.opensky-network.org/data-samples/metadata/aircraft-database-complete-2025-08.csv`
- Validation: `npm run check`, `npm test`, and `npm run build` all pass. Vite still reports the existing large main bundle warning during production build.
- Blocker/risk: The measured shard distribution is badly skewed. `a.json` came out at about `16.73 MiB` raw and `4.00 MiB` gzip, well past the HLD warning thresholds. The generator now makes that visible, but the 16-shard scheme is not actually within the stated guardrails for this dataset.
- Emotional temperature: Satisfied with the implementation quality, uneasy about shipping the 16-shard partition unchanged because the data proved the skew is real.

- Follow-up task: Revise the Step 2 partition without throwing away the existing implementation, keep it deterministic, regenerate the real shards, and revalidate.
- Decision: Switched the shard prefix from the first 1 hex character to the first 2 hex characters of `icao24`. That keeps the architecture identical and fixes the empirical size problem with the smallest plausible change.
- Implementation detail: The generator now prunes stale `.json` files in `public/aircraft-identity` that are not part of the current prefix set, so the old `a.json`/`c.json` style files do not linger after repartitioning.
- Data source re-verified: `https://s3.opensky-network.org/data-samples/metadata/aircraft-database-complete-2025-08.csv` was refreshed again into `tmp` before regeneration.
- Measured result: The new worst-case shard is `c0.json` at `2,822,182` raw bytes (`2.69 MiB`) and `577,346` gzip bytes (`563.8 KiB`). That is comfortably under the original warning thresholds.
- Validation rerun: `npm run check`, `npm test`, and `npm run build` all pass after the repartition. The same pre-existing Vite large-bundle warning still appears during `npm run build`.
- Emotional temperature: Relieved. This is the first Step 2 state that actually matches the design intent instead of merely exposing where the design was wrong.

## 2026-03-12 14:43

- Task: Apply the Stage 2 review feedback without changing the Step 2 architecture or touching Step 3.
- Fixed: the generator entry-point guard now matches both `.ts` and `.js` execution paths, with a focused unit test so the bug does not quietly come back later.
- Fixed: `buildAircraftIdentityShardUrl` was genuinely wrong for the current `vite.config.ts` setting `base: "./"`. Building the shard URL from `location.origin + BASE_URL` produced invalid or malformed URLs. I switched it to resolve against the current page URL instead, then added direct tests for both relative (`"./"`) and absolute (`"/worldviewer/"`) Vite base styles.
- Fixed: the GitHub Pages hint test in `trafficHelpers.test.ts` was misleading. It now asserts the true behavior: aircraft hints are still the same zoom hint on static hosts because the browser polls OpenSky directly.
- Added coverage: an in-flight deduplication test for `AircraftIdentityStore`, a dedicated `describe("formatShardSizeLine")` block, a generator integration row with a valid ICAO24 but blank identity fields, and a partial-header extraction test so header/index behavior is pinned down better.
- Decision: I did not widen Step 2 beyond the agreed feedback scope. I left the session-scoped no-retry policy, the prune-before-write order, the non-failing shard-size warning, and the sparse Step 3 model-key mapping unchanged. Those are real tradeoffs, but changing them here would either alter the intended Step 2 behavior or spill into Step 3 planning.
- Decision: I also left the `trafficClient.ts` stale-snapshot criticism alone. After re-reading the poll path, it did not hold up as a concrete correctness bug in the current flow because each poll already triggers its own shard load request for the current aircraft snapshot.
- Validation: focused Vitest on the touched Stage 2 files passed, then `npm test` and `npm run build` both passed. The existing Vite large-chunk warning still appears during `npm run build`.
- Emotional temperature: Satisfied. The useful review items tightened the implementation, and the URL bug was worth catching before it turned into a static-host regression.

## 2026-03-12 14:58

- Task: Apply one final narrow Stage 2 feedback pass only, with no Step 3 changes and no broader refactor.
- Fixed: `buildAircraftIdentityShardUrlFromBase` now handles the slashless static-host edge case for relative Vite bases. A current page URL like `https://0x4d44.github.io/worldviewer` is treated as the app directory, so shard requests stay under `/worldviewer/aircraft-identity/...` instead of collapsing back to site root.
- Added coverage: a direct regression test for the slashless helper case, plus a production-path test for `buildAircraftIdentityShardUrl` that stubs `BASE_URL` from the current `vite.config.ts` value (`"./"`). That closes the gap where the top-level function previously only ran under Vitest's default base.
- Cleanup: simplified `deriveRenderModelKey(aircraftTypeCode)` in the runtime merge path and removed the repeated `shards.get(prefix) ?? {}` fallback noise in the generator write loop by storing the pre-seeded shard once.
- Validation: `npx vitest run src/traffic/aircraftIdentity.test.ts scripts/generateAircraftIdentity.test.ts`, then `npm test`, then `npm run build` all pass. The same pre-existing Vite large-chunk warning still appears during `npm run build`.
- Emotional temperature: Finished and calmer. This was the last small correctness hole worth closing before Step 2 stops moving.

## 2026-03-12 15:12

- Task: Close the last devil's-advocate objections without widening Step 2.
- Fixed: the generator now refuses to run unless the output directory name is exactly `aircraft-identity`. That is intentionally blunt. The point is to make `pruneStaleShardFiles` incapable of deleting unrelated JSON files because of an operator typo like `--output public`.
- Fixed: URL resolution now prefers `document.baseURI` when available and only treats `.html` / `.htm` endings as document paths. That keeps dotted app directory names like `/worldviewer.v2` on the app sub-path instead of collapsing them back to site root.
- Added coverage: the generator integration sample now proves uppercase `icao24` rows are normalized to lowercase shard keys, there is a direct guard test for the dedicated output directory requirement, the store tests now stub `BASE_URL` explicitly, and the URL tests cover both dotted directory names and actual HTML document paths.
- Emotional temperature: better. These were the right boring safeguards to add. The feature still feels small and clean instead of padded with defensive machinery.

## 2026-03-12 15:18

- Final tidy-up: the localhost fallback URL test now explicitly stubs `document` and `location` away before asserting the fallback. That removes the accidental dependency on Vitest's default Node environment and makes the test honest about what it is checking.
- Emotional temperature: closing in. This is now test hygiene, not feature shape.
