2026-03-13 22:04 Europe/London

Task
- Combine Phase 3 and Phase 4 traffic implementation work.
- Remove the obsolete server-side aircraft relay path and leave the local relay ships-only.
- Add lifecycle coverage around the surviving relay seam instead of testing only pure relay-core helpers.

Initial assessment
- The browser already owns the live OpenSky aircraft path in `src/traffic/trafficClient.ts`; the server copy is dead weight and currently duplicates types and provider logic.
- `server/trafficRelay.ts` mixes real ship-relay lifecycle with obsolete aircraft polling, which is exactly why there is no direct server lifecycle test today.
- The worktree is noisy in unrelated traffic and aircraft files, so the safest route is a narrow edit set plus targeted validation only.

Supervisor stance
- Delete dead code rather than preserve optionality.
- Keep the relay app seam thin and concrete.
- Do not widen scope into unrelated test failures, especially the pre-existing weather radar issue.

Emotional temperature
- Focused and mildly impatient. The old aircraft relay path is obvious duplication now, and the main risk is letting cleanup sprawl into “architecture” work instead of just removing it.

Next actions
- Create one shared traffic contract file and point browser/server code at it.
- Cut aircraft state and timers out of the server relay.
- Add direct lifecycle tests for the ships-only relay app.

2026-03-13 22:10 Europe/London

Implementation result
- Removed the server OpenSky provider, its tests, and the duplicate `server/trafficModel.ts` contract file.
- Repointed the server relay and ship provider code at `src/traffic/trafficTypes.ts` so subscribe/snapshot/live-track types now come from one source.
- Simplified `server/trafficRelayCore.ts` to ship-state only:
  - no aircraft tracks
  - no OpenSky poll timing
  - aircraft requests are reported as unavailable instead of secretly doing nothing
- Extracted a thin `createTrafficRelayApp` seam in `server/trafficRelay.ts` so the real ship relay lifecycle is testable without spinning up the whole entrypoint on import.
- Added direct lifecycle coverage for:
  - aircraft-only subscriptions not opening the ship upstream
  - live ship bbox resubscribe on viewport change
  - reconnect cancellation when the last ship subscriber leaves
  - clearing ship state across a zero-subscriber restart
- Added browser-side regression coverage that the client keeps aircraft browser-direct and only sends ships through the relay even when both layers are enabled.

Validation
- Targeted Vitest passed:
  - `server/bbox.test.ts`
  - `server/providers/aisstream.test.ts`
  - `server/trafficRelayCore.test.ts`
  - `server/trafficRelay.test.ts`
  - `src/traffic/trafficClient.test.ts`
- `npm run check:types` passed.
- I did not run the unrelated whole-suite weather radar failure path because that is outside this task.

Emotional temperature
- Satisfied. This landed as a real deletion pass, not a disguised refactor, and the new relay lifecycle tests finally hit the part of the server that can actually break in production.

Remaining risks
- The relay lifecycle tests use fake sockets rather than an end-to-end `ws` server/client pair, so they cover lifecycle wiring tightly but not the full network stack.
- The shared contract now lives in `src/traffic/trafficTypes.ts`, which still contains browser-only connection-status types alongside the wire contract. That is acceptable for now, but if the contract grows again it may deserve a narrower shared module later.
