2026-03-13 18:16 Europe/London

Task
- Run a codebase sweep for tech debt, dead code, duplicate code, and obvious mistakes.
- Produce a detailed report and a follow-on action plan under `wrk_docs`.

Supervisor notes
- This is a review/documentation task, so I am keeping hands off the implementation and using agents for the actual sweep.
- I want competing perspectives, not one giant generic review. I am splitting the team into architecture/maintainability, runtime/UI correctness, tests/tooling, and devil's advocate.

Emotional temperature
- Calm, slightly wary. Whole-repo sweeps tend to generate noise unless the prompts are tightly scoped.

Next actions
- Dispatch the review agents in parallel.
- Consolidate their findings into a single report.
- Produce an action plan that cuts scope instead of inflating it.

2026-03-13 18:33 Europe/London

Review results
- The team mostly converged on a small set of real issues instead of a long vanity list, which is what I wanted.
- Architecture review found the biggest structural problem: two aircraft ingestion paths are alive in the codebase, with browser-direct OpenSky on the client and still-live aircraft relay logic on the server.
- Runtime review found the best immediate wins: misleading ships availability behavior, stale popups when only one traffic layer is disabled, search races, and vague aircraft failure status.
- Tooling review confirmed the weak test boundaries around `trafficClient.ts` and `trafficRelay.ts`, plus the fragmented validation commands and ugly repo-root clutter.
- The devil's advocate was useful. It cut down weak "file too big" claims, called out that duplicated bbox helpers were not the important duplication, and added two concrete misses: stale ship markers after relay loss and actual mojibake strings in `main.ts`.

Artifacts written
- Report saved to `C:\language\mdgpt54\worldviewer\wrk_docs\2026.03.13 - codebase tech debt sweep.md`.
- Action plan saved to `C:\language\mdgpt54\worldviewer\wrk_docs\2026.03.13 - codebase tech debt action plan.md`.

Supervisor judgment
- The plan is intentionally boring in the right way. Fix concrete correctness bugs first, make validation less slippery, then add just enough tests to let us delete the duplicate aircraft architecture safely.
- I do not want us wandering into a generic traffic framework. That would be classic fake productivity here.

Emotional temperature
- More confident now. The report feels grounded instead of hand-wavy.
- Mild irritation at the repo-root clutter. It is not the biggest problem, but it makes everything feel sloppier than it needs to.

Next actions
- Hand Arthur the saved report and plan.
- If Arthur wants implementation next, start with Phase 1 and keep the changes small.
