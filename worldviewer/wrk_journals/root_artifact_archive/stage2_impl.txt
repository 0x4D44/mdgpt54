Step 2 is implemented and the shard partition has been revised in place.

The main changes stay in the same files as the first pass: [scripts/generateAircraftIdentity.ts](C:/language/mdgpt54/worldviewer/scripts/generateAircraftIdentity.ts), [src/traffic/aircraftIdentity.ts](C:/language/mdgpt54/worldviewer/src/traffic/aircraftIdentity.ts), [src/traffic/aircraftIdentityData.ts](C:/language/mdgpt54/worldviewer/src/traffic/aircraftIdentityData.ts), [README.md](C:/language/mdgpt54/worldviewer/README.md), and the regenerated static output under [public/aircraft-identity](C:/language/mdgpt54/worldviewer/public/aircraft-identity). The runtime shape is unchanged: visible aircraft still trigger on-demand shard fetches into a session-only cache, identity fields still merge into current visible aircraft, and Step 3 remains untouched.

Revision summary:
- The static identity partition now keys by the first two hex characters of `icao24`.
- Shards are now `00.json` through `ff.json` instead of `0.json` through `f.json`.
- The generator prunes stale legacy shard files before writing the current shard set, so the output directory stays reviewable after repartitioning.
- Tests and README/HLD notes now match the two-hex shard scheme.

Data source used for this pass:
`https://s3.opensky-network.org/data-samples/metadata/aircraft-database-complete-2025-08.csv`

Measured shard results from the regenerated real dataset:
- worst-case shard: `c0.json`
- `c0.json`: `39,735` entries, `2,822,182` bytes raw (`2.69 MiB`), `577,346` bytes gzip (`563.8 KiB`)
- next-largest raw shard: `40.json` at `1,638,026` bytes raw (`1.56 MiB`)
- largest `a*` shard after repartition: `a0.json` at `1,544,629` bytes raw (`1.47 MiB`) and `400,560` bytes gzip (`391.2 KiB`)
- generated output count: `256` shard files, with `0` legacy one-character shard files remaining

Validation status:
- `npm run check`: passed
- `npm test`: passed
- `npm run build`: passed

Remaining risk:
- The app still carries the pre-existing Vite warning about the large main JS bundle during production build.
