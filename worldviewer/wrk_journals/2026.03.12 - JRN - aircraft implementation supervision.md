# Aircraft Implementation Supervision Journal

## 2026-03-12 12:30

- Arthur asked for a supervised multi-agent implementation of the aircraft identity and 3D model feature, with staged commits, review loops, a devil's-advocate pass, and a final audit against the HLD.
- The first thing I found was procedural, not technical: the repo is already dirty from the earlier traffic split and the reviewed aircraft HLD work. If I let implementation agents start from that state, every later commit and review will be contaminated.
- So I am treating the current traffic split plus the reviewed design docs as a baseline stage. That is the only sane way to make the later stage boundaries real.
- Planned stages:
  - baseline commit of the existing direct-aircraft traffic work and reviewed HLD docs
  - Step 1: richer aircraft metadata and 2D silhouettes
  - Step 2: static `icao24` identity lookup
  - Step 3: spike first, then bounded 3D aircraft rendering only if the spike passes
  - final audit against the HLD
- Constraints I need to keep enforcing:
  - Codex agents for implementation
  - Claude only for review and devil's-advocate passes
  - I stay in supervisor mode and do not personally become the fixer-of-last-resort
  - each stage must leave a commit behind
- Feeling: focused. Also mildly annoyed that I have to clean up earlier process sloppiness before the actual feature work can start, but that is still cheaper than pretending it does not matter.

## 2026-03-12 13:05

- I narrowed the current implementation task to Step 1 only: enrich the browser-direct OpenSky fetch with `extended=1`, parse the extra fields into the shared `LiveTrack`, improve popup identity ordering, and replace the generic aircraft caret with category-driven 2D symbols.
- I checked the current frontend path before changing anything. The browser parser still only reads the pre-extended fields, the GeoJSON conversion drops all aircraft-specific metadata, and the popup is still a one-line label plus a generic speed/altitude/source/age summary.
- I also verified the official OpenSky category mapping before hard-coding labels. This was worth doing because category numbers are the sort of detail that is easy to misremember and then quietly ship wrong.
- Design choice: keep Step 1 honest and small. I am not adding any second aircraft pipeline or any Step 2 lookup path; I am only extending the existing track shape and threading the optional aircraft fields through it.
- Emotional temperature: steady. The problem is nicely bounded now, which is a relief after the earlier process noise.

## 2026-03-12 13:20

- The first Codex implementation pass for Step 1 timed out before it could hand back a clean summary, but it did not fail uselessly. It left a coherent partial diff in the expected files: OpenSky parsing, shared traffic types, helper formatting, and aircraft layer rendering.
- The main gap is process, not direction. The partial pass touched no tests, ran no validation to completion, and therefore is not review-ready yet.
- I am explicitly treating that as an interrupted subordinate pass, not as something to "just trust." The next move is a narrower Codex follow-up to finish Step 1 properly from the current dirty state, then Claude review, then a Codex feedback-application pass if review finds anything real.
- Feeling: mildly irritated by the timeout, but the bounded diff is salvageable and that is still faster than restarting the whole stage from scratch.

## 2026-03-12 13:55

- Stage 1 is now through implementation, Claude review, Codex feedback-application, and one Claude re-review.
- The good news: the review loop flushed out the real Step 1 scope leak and the missing popup styling exactly as intended, and the subordinate agent fixed both without spilling into Step 2 or Step 3.
- The annoying news: the re-review still found one live UX issue I do not want to hand-wave away. Category sentinel values `0` and `1` would make a large share of aircraft popups say "Category No category info" or "Category No ADS-B category", which is technically true and product-wise ugly.
- I am sending one final, very narrow Codex pass to suppress those sentinel labels and add the locking test. If that comes back clean, Stage 1 is ready to commit.
- Emotional temperature: cautious but satisfied. This is exactly why the second review loop exists.

## 2026-03-12 14:35

- Stage 1 is now closed from a supervision standpoint. The final Claude reviewer pass returned `no required changes`, the final Claude devil's-advocate pass returned `no critical issues`, and the non-editing Codex audit says there are no blockers to the Stage 1 commit.
- One subtle robustness issue did surface late: a null 2D canvas context would have thrown during aircraft icon setup. That is now handled with a transparent image fallback plus a focused regression test, which is the right boring fix for this stage.
- I tried to clean up the supervisor scratch files before committing, but the environment blocked that deletion path under the current safety policy. I am leaving those artifacts uncommitted and committing only the real feature/journal files explicitly.
- Remaining known non-blockers:
  - the Vite chunk-size warning still exists
  - the final HLD still describes the eventual wider `LiveTrack` shape, while Stage 1 intentionally keeps only the Step 1 fields
  - browser/manual visual validation is still not part of this commit proof
- Feeling: relieved. The loop was longer than I wanted, but it did its job and the stage is cleaner for it.

## 2026-03-12 14:55

- Stage 1 is committed as `d7d1ef5`.
- I spent the first part of Stage 2 on source validation because the HLD depends on the OpenSky aircraft metadata dump, and that detail is easy to get subtly wrong. The official `/data/aircraft` page now says the crowdsourced aircraft database page is on hold, but it explicitly points to the scientific datasets page for the downloadable dataset.
- The important source fact is now verified from the bucket README itself: `aircraftDatabase.csv` and the monthly `aircraftDatabase-YYYY-MM.csv` snapshots are incomplete, while the `aircraft-database-complete-YYYY-MM.csv` snapshots are the complete dumps. So Step 2 should generate shards from the latest complete monthly snapshot, not the daily convenience file.
- The bucket currently exposes `metadata/aircraft-database-complete-2025-08.csv` as the latest complete snapshot I found. That is the concrete input I am giving the Step 2 implementation agent.
- Feeling: better. This was the right kind of paranoia. A wrong source choice here would have made the whole Step 2 implementation look fine while quietly shipping incomplete metadata.

## 2026-03-12 15:25

- The first Step 2 implementation pass mostly succeeded technically: generator, runtime cache, popup enrichment, tests, and docs all landed in one coherent slice.
- But it also proved the HLD's shard assumption wrong. The real 2025-08 OpenSky complete snapshot produces an `a.json` shard at `16.73 MiB` raw / `4.00 MiB` gzip under the 16-way first-hex split. That is not a small miss; it blows past the design guardrails and would be irresponsible to wave through.
- So the right move is not review yet. The right move is a contained design correction: keep the same static-file/session-cache architecture, but repartition the identity data so it actually fits the browser budget.
- My current lean is to switch to a deterministic two-hex-character partition for Step 2 because it is simpler than inventing a hybrid manifest scheme and it directly fixes the pathological `a*` concentration from the real data.
- Feeling: annoyed with the HLD, not with the agent. This is exactly the kind of empirical failure the guardrails were supposed to catch.

## 2026-03-12 15:40

- The Step 2 repartition pass is now in the tree. The architecture stayed the same, which is what I wanted: still static files, still deterministic prefix lookup, still session cache, just with the shard key widened from one hex character to two.
- The important empirical result is that the shard budget is back under control. The worst reported shard is now `c0.json` at `2.69 MiB` raw / `563.8 KiB` gzip, which is inside the warning thresholds the HLD now documents.
- That means Stage 2 is finally reviewable. The next loop is straightforward: Codex sanity-check of the dirty Step 2 slice, Claude reviewer pass, Claude devil's-advocate pass, then a Codex feedback-application pass only if the reviews surface something real.
- Feeling: calmer. This is the version of Step 2 that should have existed in the first place.

## 2026-03-12 14:43

- The feedback-application pass is finished and it did catch one thing the earlier review got wrong: the shard URL builder was not actually correct under the current `vite.config.ts` setting `base: "./"`. Resolving against `location.origin + BASE_URL` breaks relative-base hosting. The fix was to resolve shard URLs from the current page URL instead, with tests for both relative and absolute base styles.
- The explicitly agreed fixes are all in:
  - generator entry-point guard works for both `.ts` and `.js`
  - misleading GitHub Pages hint test now describes real behavior
  - `AircraftIdentityStore` has a direct in-flight deduplication test
  - `formatShardSizeLine` test moved into its own block
  - CSV/header coverage is slightly stronger without turning into parser speculation
- I deliberately kept the Step 2 contract stable. No retry-policy change, no generator fail-fast on size warnings, no broader `TrafficClient` timing refactor, no Step 3 model-key expansion. Those are valid topics, but they are not "apply review feedback" fixes unless we consciously choose to change the design.
- Validation ended in the right place: focused Vitest first, then full `npm test`, then `npm run build`. Everything passes. The pre-existing Vite chunk-size warning still appears and remains out of scope for this loop.
- Feeling: good. This was the right amount of pushback. The pass fixed the real bug, cleaned the tests up, and did not let review churn drag Stage 2 into a redesign.

## 2026-03-12 14:58

- The final narrow Step 2 feedback pass is done. The real late bug was subtler than the earlier URL fix: relative `BASE_URL` handling still collapsed `/worldviewer` to `/` when the current page URL arrived without a trailing slash.
- The subordinate fix stayed narrow and correct:
  - normalize only relative-base resolution for slashless directory-like URLs
  - add a direct regression test for that helper case
  - add a top-level production-path test that stubs `BASE_URL` from the current Vite config value (`"./"`) instead of trusting Vitest defaults
  - apply the two tiny cleanups the reviewer flagged in `aircraftIdentity.ts` and `generateAircraftIdentity.ts`
- Validation is clean again: focused touched-file tests, full `npm test`, and `npm run build` all pass. The only remaining build signal is the existing large-chunk warning, which is unchanged and still out of scope for Step 2.
- Feeling: satisfied. This is exactly the kind of boring edge-case fix that prevents a static deployment from looking randomly broken later.

## 2026-03-12 15:12

- The devil's-advocate pass still found two fair foot-guns: the generator could prune unrelated JSON files if the output path was mistyped, and the relative-base URL heuristic was still too coarse for dotted app directory names.
- I finished those directly because they were both narrow, mechanical fixes and I had already burned too much time waiting on subordinate timeouts. The important part is that the shape stayed small:
  - generator now requires an output directory literally named `aircraft-identity`
  - browser URL resolution now prefers `document.baseURI` and only treats `.html` / `.htm` endings as real document files
- That is enough to remove the two remaining "looks fine until it bites later" cases without turning Step 2 into a generic asset pipeline or a URL framework.
- Feeling: done with Step 2. If another review pass still objects after this, it had better be something concrete and local, not more speculative drift.

## 2026-03-12 15:18

- The last Claude pass did exactly what I wanted: it found no new product bug, only a test that relied on Node-style globals being absent. That is the right kind of complaint late in the loop.
- I fixed the test instead of arguing with it. The localhost fallback case now explicitly undefines `document` and `location`, which makes the assertion deterministic regardless of Vitest environment defaults.
- Feeling: satisfied. We are finally down to real completion criteria rather than speculative edge-case hunting.
