Validate the current Stage 3 working tree in C:\language\mdgpt54\worldviewer without editing files.

Run, in order:
1. npx vitest run src/traffic/aircraft3d.test.ts src/traffic/aircraft3dLayer.test.ts
2. npm test
3. npm run build

If a command fails because of unrelated workspace noise, retry once only if that fits the repo instructions, then stop and report clearly.
Do not modify files. Do not commit.
Return a concise summary of each command result and any warnings.
