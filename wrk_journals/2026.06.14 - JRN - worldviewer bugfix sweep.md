# 2026.06.14 — JRN — Worldviewer bug-fix sweep

**Lead:** Claude (Opus 4.8), autonomous. **Branch:** `master` (Arthur: no one else on it).
**Source of bugs:** CR report `worldviewer/wrk_docs/2026.06.14 - CR - …md` + the prioritized list.
**Scope (Arthur):** Everything incl. Tier 4. **Push:** once at end, all green.
**Green gate:** `npm run check:types` + `npx vitest run` (unit). e2e (`test:e2e`) is out-of-band.
**Verification policy:** 2-of-3 skeptic votes for substantive fixes; one-liners skip the design round
(proportionality). Ships client code left untouched (deferred Decision B).

## Baseline @ 8c5f5a8
- typecheck: clean (client + scripts)
- vitest: **exit 1** — globs the 10 Playwright `e2e/*.spec.ts` (this is bug H2)
- 713 unit tests pass otherwise.

## Bug ledger
Status: ⬜ todo · 🔧 in progress · ✅ fixed (commit) · 🅿️ parked

| ID | Sev | Title | Status | Commit / note |
|----|-----|-------|--------|---------------|
| H2 | High | vitest globs e2e → `check` red | ✅ | 4748b8b — added vitest.config.ts (include unit, exclude e2e) |
| N1 | Nit | bogus `KMDK` airport dupes `KBWI` | ✅ | c3b985b — removed entry; lookup now null |
| L5 | Low | `routeCache` unbounded | ✅ | 704a430 — FIFO cap ROUTE_CACHE_MAX=256 + clearRouteCache |
| L1 | Low | `aircraftPollDelayMs` 0ms busy-spin | 🅿️ | PARKED — not reproducible. delay=0 only when `elapsed>=OPENSKY_POLL_MS`, which forces `isStale` true in pollAircraftIfNeeded → it polls (correct overdue behaviour), never the `!shouldPoll` re-arm. `!shouldPoll` ⟹ `!isStale` ⟹ `elapsed<interval` ⟹ delay>0. No busy-spin path exists. Errors use exponential backoff separately. A defensive floor would only delay a legitimately-due poll → not applied (would be fixing a non-bug). |
| M5 | Med | RainViewer host unpinned (untrusted) | ✅ | cb59ff3 — URL allowlist https *.rainviewer.com |
| L7 | Low | no CSP / referrer meta | ✅ | cbf25f1 — CSP+referrer meta + csp.test.ts anti-drift |
| L6 | Low | bookmarks/search shape unvalidated | ✅ | dbf53cb — isBookmark filter + finite coord/bbox guards |
| N2 | Nit | readout copies unwrapped longitude | ✅ | 28ad95b — wrapLongitude in both formatters |
| L3 | Low | ISS trail antimeridian gap | ✅ | c9704d8 — bridge to ±180, stop dropping vertices |
| L4 | Low | antimeridian in search fitBounds + flight-route arc | ✅ | deb7edd — unwrap arc lng + dateline fitBounds fallback |
| L2 | Low | synthetic aircraft id embeds `now` → churn | ✅ | 29a1c7c — drop rows without string icao24 |
| H1 | High | cloned 3D materials never disposed (leak) | ✅ | addd4ae — disposeClonedAircraftMaterials at removal sites |
| M2 | Med | 2D↔3D per-track handoff has no hysteresis | ✅ | f662c81 — sticky per-track hysteresis (OFF=0.8·ON) |
| M4 | Med | 3D `dispose()` never called; move handler always on | ✅ | 0bebb02 — bail syncNow on empty tracks (corrected guard; keyed on track count not `enabled`). dispose-wiring intentionally NOT added (map never recreated → would be dead code). |
| M1 | Med | solar terminator degenerates at equinox | 🅿️ | PARKED — diagnosis workflow fix refuted 0/2. The 1e-6° epsilon branch is a production no-op: runtime declination sweeps through 0.001–0.5° on the equinox where the single-valued lat(lon) ring (anchored at a pole, solarTerminator.ts:238,251-260,294-301) is ALREADY degenerate for hours. A correct fix needs a geometry reformulation (night polygon not expressed as lat(lon)) or a large epsilon that itself changes near-equinox output / adds a visible discontinuity — explicitly non-minimal and out of scope. Cosmetic overlay, ~2 days/yr. Best fix options recorded; not guessing. Also flagged: nightPoleLatitude tie-break ill-defined exactly at lat===0. |
| M10 | Med | `as any` on MapLibre expressions; dup guards | ✅ | fd84119 (expression typing) + 658e21c (guards.ts). hasSetData triplication left (out of scope, alias-name diff). |
| H6 | High | overlay polling lifecycle duplicated ×4 | ✅ | 311674d — createPollingOverlay factory; earthquake/iss/weather delegate; solarTerminator excluded; net -407 lines; existing suites unchanged + new factory test |
| M9 | Med | main.ts triplicated toggle lists, untested | ✅ | 6763e9b — sceneToggles.ts TOGGLES table + dispatchToggle; 3 main.ts sites loop the table; cameraHash BOOLEAN_KEYS/KEY_ORDER exported; 24 new tests incl 2^6 hash round-trip |

## Implementation notes
- H6 & M9 heavy rewrites delegated to focused subagents with the vetted plans + "existing tests are the oracle, change none"; lead verified `npm run check` green and reviewed the factory + main.ts diffs before committing.
- Final: `npm run check` exits 0 (39 files, 784 unit tests); `npm run build` succeeds with the new CSP. 17 atomic commits on master (4748b8b..6763e9b).

## Workflow note
- `args` object did not reach the diagnosis workflow (pipeline got non-array) → relaunched with
  `repo`/`bugs` hardcoded as literals in the script. Design workflow stopped+relaunched same way.

## Refactor design verdicts (workflow wf_3400ff68)
- **H6** createPollingOverlay factory — 2/2 skeptics sound. New `src/overlays/createPollingOverlay.ts`;
  fold earthquake/iss/weather; **exclude solarTerminator** (no fetch/abort/refreshRevision/publish; re-runs
  every enable; `else if (!existingSource)` guard; extra setGetNow). Public surface preserved → existing
  overlay tests unchanged; add factory contract test. Watch: ISS trail push+cap must stay inside injected
  sync (after isCurrentRefresh gate); weather reassertion via optional shouldReassertOnEnable/reassert hooks;
  anchor order differs (eq/iss label→road, weather road→label) — keep in injected sync.
- **M9** TOGGLES table — 1/2 sound. Dissent is a real, trivial gap: anti-drift test needs `BOOLEAN_KEYS`/
  `KEY_ORDER` **exported** from cameraHash.ts (currently private) and cameraHash.ts added to edits. Plan
  otherwise verified behaviour-preserving (9 chip toggles; 6 hash-persisted; earthquakes/iss/measure
  runtime-only — must NOT gain hash keys; traffic toggles excluded). Will incorporate the export fix.

## Independent review (workflow wf_78e57676)
3 reviewers over the sweep diff (8c5f5a8..HEAD): geo/validation **clean**, traffic/3D **clean**,
refactors/config found 2 items — both behaviour-preserving-but-implicit invariants, confirmed
**unreachable** in this single-map app: (1) M9 toggle-effect/status-pill ordering, (2) H6 cross-map
reassert dropped. No regressions. Documented both as explicit invariants → commit 33e0548.

## Final state
- 18 commits on master: 4748b8b..33e0548. `npm run check` exits 0 (39 files, 784 unit tests).
  `npm run build` clean (incl. new CSP). Pushed at end.
- Parked: **L1** (busy-spin not reproducible — proven), **M1** (equinox terminator — epsilon fix is a
  production no-op; needs geometry reformulation, out of minimal scope).
- Deploy note: L7's CSP lives in index.html → rebuild + copy dist/* into 0x4d44.github.io/worldviewer/
  to take effect on Pages (not done — deploy step, not requested).

## Log
(per-bug entries are captured in the ledger above + the commit messages)
